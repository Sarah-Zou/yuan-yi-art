node_modules/
.next/
out/
*.log
.env*
.DS_Store

# Optimized image variants generated by scripts/optimize-images.mjs.
# Regenerated on every build; lib/imageManifest.json is committed so the
# manifest import always type-checks on a fresh clone.
public/images-optimized/
